# OmniPost 后端依赖（锁定版本，便于迁移）
# 安装：python3 -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt

# ── Web 框架 ──
fastapi==0.115.0
uvicorn[standard]==0.30.0
python-multipart==0.0.29

# ── 数据库 ──
sqlalchemy[asyncio]==2.0.35
aiosqlite==0.20.0
alembic==1.13.2

# ── 配置/校验 ──
pydantic==2.9.0
python-dotenv==1.0.1
pyyaml==6.0.2

# ── HTTP 客户端 ──
httpx==0.27.0
requests==2.34.2

# ── Excel 导入导出 ──
openpyxl==3.1.5

# ── TTS ──
edge-tts==7.2.8

# ── 测试 ──
pytest==9.0.3
pytest-asyncio==1.3.0
feedparser==6.0.12
